/*
Transforma la siguiente función a una arrow function de una línea. 
Este ejercicio debe quedar en un archivo llamado `2_arrow.js` (1 Punto)

let suma = function(a, b){
return a + b
}

 */

//Respuesta:

/* //En primer lugar, transformamos la función propuesta a función tipo arrow multilínea.

let suma = (a, b) => {
    return a + b
}

*/

// Se procede a transformar la arrow function multilínea a una arrow function de una línea.

let suma = (a, b) => a + b

// Se hace notar que al hacer una arrow function de una línea se omite la palabra reservada return
